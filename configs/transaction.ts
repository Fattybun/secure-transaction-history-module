import { generateMockTransactions } from "@/helper/generateMockTransaction";
import { transaction } from "@/interface/transaction";

export const MOCK_TRANSACTION: transaction[] = generateMockTransactions();
